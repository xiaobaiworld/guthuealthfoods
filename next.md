# 下一步行动：5.1 Google Search Console / GA4 接入准备

**任务目标：**
在 `v0.0.5` 已完成内容路由与 SEO 基线的前提下，准备后续上线所需的 Google 接入条件，但仅在确实需要用户提供域名、DNS 或 GA4 信息时再打断用户。

**执行动作：**
1. 整理正式生产域名占位与环境变量要求，避免在接入 Google 前改动多处代码。
2. 预留 GA4 初始化位置与事件命名方案，但不写入虚假 ID。
3. 补充 Search Console、Sitemap 提交与 DNS 验证的执行清单。
4. 在需要真实外部信息时，再向用户发起一次集中式输入请求。
